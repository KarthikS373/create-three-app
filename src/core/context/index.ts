const id = ""
const projectName = ""
const projectPath = ""
const description = ""

const isTemplate = false
const template = 0

const stack = "vanilla"
const physics = "cannon"
const language = "javascript"

const APIKeys = {
  CLIENT_SECRET: "",
}
const modules = null

const context: Project = {
  id: id,
  name: projectName,
  path: projectPath,
  description: description,
  isTemplate: isTemplate,
  template: template,
  stack: stack,
  physics: physics,
  language: language,
  modules: modules,
  apiKeys: APIKeys,
}

export default context
